# Concepts Overview

The concepts library explains the architectural foundations that cut across Ev2.Cpu modules. Use these documents alongside the detailed specifications in `../specs/`.

- `ARCHITECTURE.md` – System context, dependency graph, deployment views.
- `platform-roadmap.md` – Cross-module roadmap and sequencing guidance.

For historical background, check Git history or `docs/.meta/doc-registry.yaml`.
