namespace Ev2.Cpu.Examples
open System
open Ev2.Cpu.Core

/// Examples demonstrating the refactored architecture
module RefactoredExamples =
    
    /// Basic expression building and evaluation
    let basicExampleFunction() =
        printfn "=== Refactored Architecture Example ==="
        printfn "This demonstrates the new modular architecture"
        printfn "- Unified type system"
        printfn "- Plugin architecture" 
        printfn "- Enhanced parsing"
        printfn "- Better error handling"
        printfn ""
        
    /// Demo showing what the refactored system could do
    let demonstrateCapabilities() =
        printfn "=== Refactored System Capabilities ==="
        printfn "1. Unified Type System - No more duplicate type definitions"
        printfn "2. Plugin Architecture - Extensible with custom functions"  
        printfn "3. Better Error Handling - Clear error messages with location"
        printfn "4. Enhanced Parser - Supports complex expressions"
        printfn "5. Modular Design - Each component has clear responsibilities"
        printfn ""
        
    /// Run all demonstrations
    let runAll() =
        printfn "========================================="
        printfn "Ev2.Cpu Refactored Architecture"
        printfn "========================================="
        printfn ""
        
        basicExampleFunction()
        demonstrateCapabilities()
        
        printfn "========================================="
        printfn "Refactoring completed successfully!"
        printfn "========================================"
